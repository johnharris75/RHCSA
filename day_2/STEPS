#Notes for Day 2
#Create a container with podman for  apache and open port 80
#Run script from a container


#pull ubunut image
pull an image - podman pull ubuntu

#create container
podman create --name ubuntu ubuntu
posman create --name <containername> <imagename>

#check podman process
podman -ps -a 

#start container
podman start <container-name>

#start a container with an interacitve shell
podman run -it --name mycontainer ubuntu bash
#podman run -it --name <containername> <image name> bash

#copy script from here to container
podman cp hello.sh mycontainer3:/tmp/hello.sh
#if container is stopped
podman start mycontainer3
$then you can execute it
podman exec -it mycontainer3 bash

#setup timer and service
#
#cd /etc/systemd/system
vi ex200script.service
[Unit]
Descrption= EX200 timer Script

[Service]
Type=oneshot
ExecStart=/home/john/repo_projects/RHCSA/day_2/testtimer.sh

vi ex200script.timer
[Unit]
Description=Run my custom ex200 script on a timer

[Timer]
#OnCalendar=hourly
#You can use any schedule (Daily, Weekly, or exact cron-like patterns, for example
#OnCalendar=daily
#OnCalendar=Mon..Fri 09:00
#OnCalendar=*-*-* *:00:15 (every 15 minutes)
#OnCalendar=*-*-01 00:00 (monthly)

OnCalendar=*-*-* *:00/15
Persistent=true

[Install]
WantedBy=timers.target

systemctl daemon-reload

systemctl enable --now ex200script.sh

#setup hourly cron to remind me to push to git
crontab -e
#cron to remind me to push git changes

0 * * * * /usr/bin/wall "Dont forget to push changes to Git at end of day"

#accept wall messages
mesg y

#check to see if you can accept wall messages
mesg
